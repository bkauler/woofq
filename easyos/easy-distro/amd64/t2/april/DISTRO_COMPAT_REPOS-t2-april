#DISTRO_BINARY_COMPAT=t2 DISTRO_COMPAT_VERSION=april

#where to get pkg docs, format: domain|uri|localfilename...

PKG_DOCS_DISTRO_COMPAT="ibiblio.org|http://distro.ibiblio.org/quirky/quirky6/amd64/packages/t2/Packages-${DISTRO_BINARY_COMPAT}-${DISTRO_COMPAT_VERSION}-official|Packages-${DISTRO_BINARY_COMPAT}-${DISTRO_COMPAT_VERSION}-official"

#note, the local pkgs database file (or partname) is also appended to these entries, so know which url to download a particular pkg from (glob wildcard allowed)...
#note, didn't put "april" subdir here, as it is specified in the db entries...

REPOS_DISTRO_COMPAT="ibiblio.org|http://distro.ibiblio.org/quirky/quirky6/amd64/packages/t2|Packages-${DISTRO_BINARY_COMPAT}-${DISTRO_COMPAT_VERSION}-official"

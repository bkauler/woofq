
#170527 oe pyro
[ -d etc/init.d ] && rm -rf etc/init.d

#want to export variable to be used by xbps-create...
echo "SHLIB_PROVIDES='libulockmgr.so.1 libfuse.so.2'" > /tmp/woofV/pt-fuse
#...read by 5populate-rootfs


if [ -e usr/lib/libical.so.1 ];then
 [ ! -e usr/lib/libical.so.0 ] && ln -s libical.so.1 usr/lib/libical.so.0
fi
if [ -e usr/lib/libicalss.so.1 ];then
 [ ! -e usr/lib/libicalss.so.0 ] && ln -s libicalss.so.1 usr/lib/libicalss.so.0
fi

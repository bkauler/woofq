
#quirky tahr, based on ubuntu trusty tahr debs.
#using my own pet 'parted' pkg, keeping this ubuntu deb but only want the libs...
[ -d sbin ] && rm -rf sbin
[ -d bin ] && rm -rf bin
[ -d usr/sbin ] && rm -rf usr/sbin
[ -d usr/bin ] && rm -rf usr/bin

#!/bin/sh

export TEXTDOMAIN=osmo-run
export OUTPUT_CHARSET=UTF-8

M1="$(gettext 'Osmo calendar and organizer is already running. Please click on the Osmo icon in the tray to pop it up.')"
popup "background=#80ff80 terminate=6 timecount=dn name=osmo-run|<big><big>${M1}</big></big>"
